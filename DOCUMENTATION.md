# Svu Ai Studio - التوثيق الشامل

## نظرة عامة

**Svu Ai Studio** هو موقع ويب متكامل (Full-Stack) مصمم خصيصاً لخدمة طلاب الجامعة الافتراضية السورية. يدمج الموقع الذكاء الاصطناعي بشكل عميق في جميع جوانب العملية الدراسية، ويوفر مصدراً واحداً لجميع احتياجات الطالب الدراسية.

## المميزات الرئيسية

### 1. مساعد ذكي بالذكاء الاصطناعي (AI Assistant)
- مساعد شخصي مدعوم بـ GPT-4
- يفهم السياق الأكاديمي
- يجيب على الأسئلة بشكل شامل وسهل الفهم
- يدعم اللغة العربية والإنجليزية

### 2. تحليل وملخصات ذكية (Smart Summarization)
- رفع ملفات PDF والمستندات
- توليد ملخصات تلقائية
- استخراج النقاط الرئيسية
- تحليل عميق للمحتوى

### 3. توليد الأسئلة والاختبارات (Quiz Generation)
- إنشاء كويزات واختبارات تفاعلية تلقائياً
- أنواع متعددة من الأسئلة (اختيار من متعدد، صح وخطأ، إجابة قصيرة)
- مستويات صعوبة مختلفة
- شروحات للإجابات الصحيحة

### 4. خطط الدراسة المخصصة (Study Plans)
- إنشاء خطط دراسية مخصصة
- تكيف مع الوقت المتاح والسرعة
- توصيات ذكية
- تذكيرات تلقائية

### 5. تتبع الأداء والتوصيات (Performance Analytics)
- تتبع التقدم الأكاديمي
- تحليلات ذكية
- توصيات شخصية لتحسين نقاط الضعف

### 6. المكتبة الرقمية (Digital Library)
- مستودع شامل للمواد الدراسية
- أدوات بحث متقدمة
- تصنيف ذكي للمواد

## البنية التقنية

### الواجهة الأمامية (Frontend)
- **إطار العمل:** React.js
- **اللغة:** TypeScript
- **أداة البناء:** Vite
- **مكتبات الواجهة:** Radix UI, Tailwind CSS
- **الرسوم المتحركة:** Framer Motion
- **إدارة الحالة:** TanStack React Query

### الواجهة الخلفية (Backend)
- **بيئة التشغيل:** Node.js
- **إطار العمل:** Express.js
- **اللغة:** TypeScript
- **قاعدة البيانات:** PostgreSQL مع Drizzle ORM
- **المصادقة:** Passport.js مع OpenID Connect
- **معالجة الملفات:** Multer
- **الذكاء الاصطناعي:** OpenAI API (GPT-4)

## المسارات (Routes) الرئيسية

### مسارات المصادقة
- `GET /api/auth/user` - الحصول على بيانات المستخدم الحالي

### مسارات المستندات
- `POST /api/documents/upload` - رفع مستند جديد
- `GET /api/documents` - الحصول على قائمة المستندات
- `DELETE /api/documents/:id` - حذف مستند

### مسارات البطاقات الدراسية
- `GET /api/flashcards` - الحصول على البطاقات الدراسية
- `GET /api/flashcards/due` - الحصول على البطاقات المستحقة للمراجعة
- `POST /api/flashcards` - إنشاء بطاقة دراسية جديدة
- `PUT /api/flashcards/:id/review` - تحديث حالة المراجعة

### مسارات جلسات الدراسة
- `GET /api/study-sessions` - الحصول على جلسات الدراسة
- `POST /api/study-sessions` - إنشاء جلسة دراسية جديدة

### مسارات الذكاء الاصطناعي الأساسية
- `POST /api/ai/chat` - محادثة مع المساعد الذكي
- `GET /api/ai/conversations` - الحصول على سجل المحادثات
- `POST /api/plagiarism/check` - فحص الانتحال

### مسارات الذكاء الاصطناعي المتقدمة
- `POST /api/ai/generate-quiz` - توليد أسئلة الاختبار
- `POST /api/ai/study-plan` - إنشاء خطة دراسية
- `POST /api/ai/recommendations` - الحصول على توصيات شخصية
- `POST /api/ai/summarize-advanced` - تلخيص متقدم للنصوص
- `POST /api/ai/answer-question` - الإجابة على سؤال مع السياق
- `POST /api/ai/evaluate-work` - تقييم العمل المكتوب

## الصفحات الرئيسية

### صفحة الهبوط (Landing Page)
- عرض شامل لميزات الموقع
- دعوة للعمل (Call to Action)
- شهادات الطلاب
- معلومات التسعير

### لوحة التحكم (Dashboard)
- نظرة عامة على الأنشطة
- أدوات الذكاء الاصطناعي المتقدمة
- إحصائيات الأداء

### المكتبة (Library)
- إدارة المستندات
- البحث والتصفية
- تحميل وحذف الملفات

### البطاقات الدراسية (Flashcards)
- إنشاء وإدارة البطاقات
- نظام المراجعة الذكي
- تتبع التقدم

## متطلبات التثبيت

### المتطلبات الأساسية
- Node.js 18 أو أحدث
- npm أو yarn
- PostgreSQL 12 أو أحدث

### متغيرات البيئة المطلوبة

```env
# OpenAI API
OPENAI_API_KEY=your_openai_api_key

# قاعدة البيانات
DATABASE_URL=postgresql://user:password@localhost:5432/svu_ai_studio

# المصادقة
OAUTH_CLIENT_ID=your_oauth_client_id
OAUTH_CLIENT_SECRET=your_oauth_client_secret
OAUTH_REDIRECT_URL=http://localhost:5000/api/auth/callback
```

## خطوات التثبيت

### 1. استنساخ المستودع
```bash
git clone <repository-url>
cd svu-ai-studio
```

### 2. تثبيت الاعتماديات
```bash
npm install
```

### 3. إعداد قاعدة البيانات
```bash
npm run db:push
```

### 4. إنشاء ملف .env
```bash
cp .env.example .env
# ثم قم بتحرير الملف وإضافة البيانات المطلوبة
```

### 5. تشغيل الخادم في وضع التطوير
```bash
npm run dev
```

سيكون الموقع متاحاً على `http://localhost:5000`

## البناء والنشر

### بناء المشروع للإنتاج
```bash
npm run build
```

### تشغيل الخادم في وضع الإنتاج
```bash
npm run start
```

## الميزات الأمنية

- المصادقة الآمنة مع OpenID Connect
- تشفير كلمات المرور
- حماية ضد CSRF
- التحقق من صحة المدخلات
- معالجة آمنة للملفات المرفوعة

## أفضل الممارسات

### للمطورين
1. استخدم TypeScript لضمان سلامة النوع
2. اتبع معايير الكود المعرفة
3. اكتب اختبارات للميزات الجديدة
4. وثق التغييرات الكبيرة

### للمستخدمين
1. استخدم كلمات مرور قوية
2. لا تشارك بيانات اعتماد الحساب
3. احذر من الرسائل المريبة
4. أبلغ عن المشاكل الأمنية فوراً

## استكشاف الأخطاء

### المشكلة: فشل الاتصال بقاعدة البيانات
**الحل:** تحقق من متغير `DATABASE_URL` وتأكد من أن قاعدة البيانات تعمل بشكل صحيح

### المشكلة: فشل توليد الأسئلة
**الحل:** تحقق من مفتاح OpenAI API وتأكد من أن لديك رصيد كافي

### المشكلة: بطء الموقع
**الحل:** تحقق من استهلاك الموارد وحاول تحسين استعلامات قاعدة البيانات

## الدعم والمساعدة

للحصول على الدعم:
1. تحقق من التوثيق
2. ابحث في المشاكل المعروفة
3. أنشئ مشكلة جديدة مع التفاصيل الكاملة

## الترخيص

هذا المشروع مرخص تحت MIT License

## المساهمة

نرحب بالمساهمات! يرجى اتباع الخطوات التالية:
1. Fork المستودع
2. أنشئ فرع للميزة الجديدة
3. اكتب الاختبارات
4. أرسل Pull Request

## الخارطة الطريقية المستقبلية

- [ ] دعم الفيديو التعليمي
- [ ] نظام التنبيهات المتقدم
- [ ] تطبيق الهاتف المحمول
- [ ] التكامل مع أنظمة إدارة التعلم الأخرى
- [ ] دعم اللغات الإضافية
- [ ] نظام الشهادات

---

**آخر تحديث:** أكتوبر 2025
**الإصدار:** 1.0.0

